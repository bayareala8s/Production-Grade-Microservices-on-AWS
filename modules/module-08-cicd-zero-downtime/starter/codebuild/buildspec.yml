version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - python -m pip install --upgrade pip
      - pip install -r requirements.txt -r requirements-dev.txt
  build:
    commands:
      - pytest -q
      # build/push image steps would go here (ECR login, docker build, docker push)


